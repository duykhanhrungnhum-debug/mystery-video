name: Hidden Beyond Processor Archived

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "BOT1_ARCHIVED"
          echo "This legacy workflow is retained for reference only. Bot2 owns production."
